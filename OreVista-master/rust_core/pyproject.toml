[build-system]
requires      = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name            = "rust_core"
version         = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
module-name = "rust_core"
features    = ["pyo3/extension-module"]
